import { StyleSheet } from 'react-native-web'

const styles = StyleSheet.create({
  container: {
    marginTop: 15
  },
  titleText: {
    color: '#fff',
    marginBottom: 5
  },
  pickerContainer: {
    backgroundColor: '#fff',
    borderRadius: 5
  },
  pickerStyle: {
    height: 60,
    color: '#000',
    borderRadius: 5,
    paddingHorizontal: 5
  }
})

export default styles
